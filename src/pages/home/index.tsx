import ChatBox from "@/common/components/chat-box";
import { FC } from "react";

interface HomeProps {}
const Home: FC<HomeProps> = ({}) => {
  return (
    <>
     <ChatBox/>
    </>
  );
};

export default Home;
